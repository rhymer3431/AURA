# Isaac Aura Live Smoke Extension

This extension exposes the live smoke runner inside an existing Isaac Sim Full App / Kit process.

It reuses `apps.editor_smoke_entry` so editor-assisted and extension-mode diagnostics share the same bootstrap phases, compatibility report, and smoke tier summary.
